
namespace fire.model


/#
 Describes the type of interest rate applied to the security.
 #/
enum security_rate_type {
    combined( "combined" ) 
    `fixed`( "fixed" ) 
    tracker( "tracker" ) 
    variable( "variable" ) 
}

         
